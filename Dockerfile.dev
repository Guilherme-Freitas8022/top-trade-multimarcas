# Desenvolvimento — node_modules em volume nomeado, nunca na pasta do projeto
# Ver Escritorio/Boas_Praticas/boas_praticas_docker_nextjs.md

FROM node:20-alpine
WORKDIR /app
EXPOSE 3000
CMD ["sh", "-c", "npm install && npm run dev"]
