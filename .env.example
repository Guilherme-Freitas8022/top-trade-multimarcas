# Variáveis de ambiente — Top Trade Multimarcas
# Nenhuma obrigatória para rodar localmente nesta fase (sem banco/API externa).
# Preencher quando houver integração (ex: formulário via n8n, analytics).

# NEXT_PUBLIC_SITE_URL=https://www.toptrademultimarcas.com.br
